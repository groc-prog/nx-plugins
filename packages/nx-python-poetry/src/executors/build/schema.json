{
  "$schema": "http://json-schema.org/schema",
  "title": "Builds the current project and all if it's local dependencies.",
  "description": "Builds the current project and all if it's local dependencies into a single wheel/package.",
  "type": "object",
  "properties": {
    "ignorePaths": {
      "type": "array",
      "description": "Ignored folders/files on build process.",
      "default": [],
      "items": {
        "type": "string"
      }
    }
  },
  "required": []
}
