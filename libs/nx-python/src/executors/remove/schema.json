{
  "$schema": "http://json-schema.org/schema",
  "title": "Remove executor",
  "description": "",
  "type": "object",
  "properties": {
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Dependencies to remove."
    },
    "local": {
      "type": "boolean",
      "description": "Whether the dependencies are local libs or not.",
      "default": false
    }
  },
  "required": ["dependencies"]
}
