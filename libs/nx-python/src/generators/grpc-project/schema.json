{
  "$schema": "http://json-schema.org/schema",
  "$id": "gRPCProject",
  "title": "Generate a new gRPC project using Poetry.",
  "description": "Generates a new, empty gRPC project with all necessary dependencies using Poetry.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the project.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Name of the gRPC service."
    },
    "description": {
      "type": "string",
      "description": "Project description.",
      "default": ""
    },
    "port": {
      "type": "number",
      "description": "Port the application will run on.",
      "default": 50051
    },
    "host": {
      "type": "string",
      "description": "Host the application will run on.",
      "default": "[::]"
    },
    "includeDockerFile": {
      "type": "boolean",
      "description": "Whether to generate a Docker file or not.",
      "default": true
    }
  },
  "required": [
    "name"
  ]
}
